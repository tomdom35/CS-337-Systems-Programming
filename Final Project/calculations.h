#include <iostream>
#ifndef CALCULATIONS_H
#define CALCULATIONS_H
#endif

using namespace std;

int find_max(int,int);

void foil(int x[]);

void factor(int x[],int);

int num_factor(int*);

int find_num_multiples(int);

void find_multiples(int*,int,int);
